# Task: Build Organization Screen bằng React Flow

## db

model OrganizationType {
  id               
  name             
  description      
  createdAt         
  updatedAt         
  createdByUserId   
  updatedByUserId   
}

## 1. Context

Tôi muốn xây dựng chức năng cho OrganizationType có CRUD là menu cấp 2
thuộc menu Organization cấp 1

cần các page sau: list, ceated, update

note: 
 + tạo compoent common cho chứ năng onContextMenu (click chuộc phải)
 + tạo hoock comon cho chức năng on key donown khi bấm nút xuống, trái, lên, phải thì có thể di chuyển focus giữa cả input (hock di chuyển focus vào input)

1. các thành phần màn hình list 
 - kế thừa layout mà spec front có miêu tả chứng năng chung trong màn hình list
 - trong header table: gồm input (có thể check all, đang check all click vào remove all)
 - onContextMenu hiển thị menu có list items gồm: 
 [
  - create ( redirect chuyển đến page create )
  - update ( những items checked gắn vào redux golobal, có stage gồm thành phần [ value: [...input checked], filed: 'organization_type_checked' ] )
  - delete ( hiển thị poup comfrim sử dụng material UI & taiwin, khi comfrim dùng những items checked call API, chỉ call API 1 lần truyền ids: [...array] xóa hàng loạt)
 ]

Màn hình ceated 

- theo layout đang mô tả layout chung trong spec
- bên phải có button submit (disabled khi invalid từ react from)
- layout dạng table, head có (có thể check all, đang check all click vào remove all), trong head là tên filed 
- tbody gồm các input nhập value, có react from và zood valid filed, message khi lỗi hiện dưới input
- có onContextMenu hiển thị menu có list items gồm: 
 [
  - submit hiển thị comfrim, comfrim ok call api, xây dựng common hiển thị component loading cho toàn màn hình lấy ý tưởng gai diện loadding từ LoadingState from C:\test-project\frontend\src\shared\components\PageStates.tsx, sau đó redirex đến page list
  - created items (thêm row để nhập input)
  - delete ( remove những itemd có check)
 ]
- use hoock (hock di chuyển focus vào input, key donown khi bấm nút xuống, trái, lên, phải)

Màn hình update

- call api 1 lần, get value từ stage golbal, get ids, sau đó call APIm truyền array ids vào api
- về các chức năng và layout tương tự như màn hình created, nhưng items get từ API call ra



# list api

- tôi muốn trong base serve bạn thêm các api để có thể kết thừa:

- findByids ()