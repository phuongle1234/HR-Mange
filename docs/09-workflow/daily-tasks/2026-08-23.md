# Task giao ngày 2026-08-23

- Tôi muốn bạn xây dựng module cho Organization
- Task này build API, gồm các endpoit sau (createMany, updateMany, getAll, DeleteMany)

- Sau đó tạo file .http (tổng hợp những api bạn đã làm)

model Organization {
  id              Int    @id @default(autoincrement())
  code            String @unique
  name            String
  description     String?

  parentId        Int?   @map("parent_id")
  parent          Organization? @relation(
    "OrganizationHierarchy",
    fields: [parentId],
    references: [id],
    onDelete: SetNull
  )
  children        Organization[] @relation("OrganizationHierarchy")

  type            OrganizationType @default(DEPARTMENT)
  sortOrder       Int @default(0)
  isActive        Boolean @default(true)

  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @updatedAt @map("updated_at")
  createdByUserId String? @map("created_by_user_id")
  updatedByUserId String? @map("updated_by_user_id")

  @@index([parentId])
  @@map("organizations")
}

enum OrganizationType {
  COMPANY
  BRANCH
  DIVISION
  DEPARTMENT
  TEAM
}

## Việc cần làm
- [ ] build api cho module organization

## Ghi chú / bối cảnh thêm
